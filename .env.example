# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database_name"

# JWT Configuration (Legacy - KMS is preferred for production)
JWT_SECRET="your-super-secure-jwt-secret-at-least-32-characters-long"

# Key Management System (KMS) Configuration
# Required for production deployment with enhanced security
KMS_MASTER_KEY="your-ultra-secure-master-key-at-least-64-characters-for-production-security"
KMS_DERIVATION_SALT="your-unique-salt-for-key-derivation-16-chars-minimum"

# External API Configuration
GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# Environment Configuration
NODE_ENV="development"  # or "production"

# Optional: Client URL for CORS (production)
CLIENT_URL="https://your-domain.com"

# Production Security Notes:
# - KMS_MASTER_KEY: Must be at least 64 characters in production
# - KMS_DERIVATION_SALT: Should be unique per deployment/environment
# - Never commit actual secrets to version control
# - Use different keys for development vs production environments
# - Keys are automatically rotated every 24 hours in production